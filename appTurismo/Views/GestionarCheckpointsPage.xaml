<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="appTurismo.Views.GestionarCheckpointsPage"
             Title="Control de Checkpoints">

    <Grid RowDefinitions="Auto, *">
        <Label Grid.Row="0" Text="Ruta del Viaje" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,15"/>

        <ActivityIndicator Grid.Row="1" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HorizontalOptions="Center" VerticalOptions="Center" Color="#2196F3"/>

        <CollectionView Grid.Row="1" ItemsSource="{Binding ListaCheckpoints}" Margin="15">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" Padding="15" CornerRadius="10" BackgroundColor="#F5F5F5" BorderColor="#ccc">
                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                                <Label Text="{Binding Nombre}" FontSize="16" FontAttributes="Bold" TextColor="#333" />

                                <Label Text="Pendiente" TextColor="#F44336" FontSize="12" FontAttributes="Bold">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding Completado}" Value="True">
                                            <Setter Property="Text" Value="¡Completado!" />
                                            <Setter Property="TextColor" Value="#4CAF50" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </VerticalStackLayout>

                            <Button Grid.Column="1" Text="✅ Marcar" 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.MarcarCompletadoCommand}"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="#2196F3" TextColor="White" 
                                    CornerRadius="5" HeightRequest="40">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding Completado}" Value="True">
                                        <Setter Property="IsEnabled" Value="False" />
                                        <Setter Property="BackgroundColor" Value="#ccc" />
                                        <Setter Property="Text" Value="Listo" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>